# v1 — library wrappers
fastapi
pydantic>=2.0
lingua-language-detector>=2.0.2
fasttext-langdetect>=1.0.5
numpy

# v2 / v3 — rule-based pipeline
underthesea

# v4 — Lingua-centric (also reused by v1's default engine)
# lingua-language-detector is already pinned above

# testing
pytest
